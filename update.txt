git config --global user.name "Tuan Anh"
git config --global user.email "ntanh80#gmail.com"

git init

git add .
git commit -m "Cập nhật code"

git remote add origin https://github.com/ntanh80/nhapmonlaptrinh.git
git pull --rebase origin master
git push -u origin master

Ghi ddeef 
git push -f origin master

-----------
Làm việc với nhánh
git checkout -b main

Gộp nhánh master vào nhánh hiện tại
git pull origin master 

git pull
